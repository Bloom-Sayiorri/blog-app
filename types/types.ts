export type User = {
    username: string;
    eamil: string;
    password: string;
    age: number;
    avatar: string;
    bio: string;
}

export type Blog = {
    title: string;
    category: string;
    content: string;
}